describe('global.sinon', () => {
  it('should exist', () => {
    expect(sinon).to.exist;
  });
});

describe('global.sandbox', () => {
  it('should exist', () => {
    expect(sandbox).to.exist;
  });
});
